# MST-SSone
This is the GitHub repository for a MiSTer core designed around the hardware of the SuperStation one FPGA console, to push the hardware to it's limits to create an entirely new gaming platform based around a "soft-core" of an open standard (RISC-V).
